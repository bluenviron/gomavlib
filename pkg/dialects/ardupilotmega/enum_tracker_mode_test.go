//autogenerated:yes
//nolint:revive,govet,errcheck
package ardupilotmega_test

import (
	"testing"

	"github.com/stretchr/testify/require"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/ardupilotmega"
)

func TestEnum_TRACKER_MODE(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.TRACKER_MODE
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.TRACKER_MODE_MANUAL.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.TRACKER_MODE
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.TRACKER_MODE_MANUAL, dec)
	})
}
