//autogenerated:yes
//nolint:revive,govet,errcheck
package common_test

import (
	"testing"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/common"
	"github.com/stretchr/testify/require"
)

func TestEnum_CAMERA_MODE(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.CAMERA_MODE
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.CAMERA_MODE_IMAGE.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.CAMERA_MODE
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.CAMERA_MODE_IMAGE, dec)
	})
}
