//autogenerated:yes
//nolint:revive,govet,errcheck
package common_test

import (
	"testing"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/common"
	"github.com/stretchr/testify/require"
)

func TestEnum_FENCE_MITIGATE(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.FENCE_MITIGATE
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.FENCE_MITIGATE_UNKNOWN.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.FENCE_MITIGATE
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.FENCE_MITIGATE_UNKNOWN, dec)
	})
}
