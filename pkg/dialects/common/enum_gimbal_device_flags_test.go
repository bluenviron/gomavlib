//autogenerated:yes
//nolint:revive,govet,errcheck
package common_test

import (
	"testing"

	"github.com/stretchr/testify/require"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/common"
)

func TestEnum_GIMBAL_DEVICE_FLAGS(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.GIMBAL_DEVICE_FLAGS
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.GIMBAL_DEVICE_FLAGS_RETRACT.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.GIMBAL_DEVICE_FLAGS
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.GIMBAL_DEVICE_FLAGS_RETRACT, dec)
	})
}
