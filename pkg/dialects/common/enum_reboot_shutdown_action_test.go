//autogenerated:yes
//nolint:revive,govet,errcheck
package common_test

import (
	"testing"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/common"
	"github.com/stretchr/testify/require"
)

func TestEnum_REBOOT_SHUTDOWN_ACTION(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.REBOOT_SHUTDOWN_ACTION
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.REBOOT_SHUTDOWN_ACTION_NONE.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.REBOOT_SHUTDOWN_ACTION
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.REBOOT_SHUTDOWN_ACTION_NONE, dec)
	})
}
