//autogenerated:yes
//nolint:revive,govet,errcheck
package common_test

import (
	"testing"

	"github.com/stretchr/testify/require"

	dialectpkg "github.com/bluenviron/gomavlib/v4/pkg/dialects/common"
)

func TestEnum_WINCH_ACTIONS(t *testing.T) {
	t.Run("zero", func(t *testing.T) {
		var e dialectpkg.WINCH_ACTIONS
		e.UnmarshalText([]byte{})
		e.MarshalText()
		e.String()
	})

	t.Run("first entry", func(t *testing.T) {
		enc, err := dialectpkg.WINCH_RELAXED.MarshalText()
		require.NoError(t, err)

		var dec dialectpkg.WINCH_ACTIONS
		err = dec.UnmarshalText(enc)
		require.NoError(t, err)

		require.Equal(t, dialectpkg.WINCH_RELAXED, dec)
	})
}
